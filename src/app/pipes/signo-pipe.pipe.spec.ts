import { SignoPipePipe } from './signo-pipe.pipe';

describe('SignoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SignoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
